'use client';

// 마이페이지 비밀번호 모달 내용
export function MyPageWrongPW({ mainTitle }: { mainTitle: string }) {
  return (
    <>
      <span className=' flex items-center justify-center text-center text-[1rem] text-black'>{mainTitle}</span>
    </>
  );
}
